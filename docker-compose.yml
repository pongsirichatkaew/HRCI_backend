
version: '2'

services:
    hrci_stage:
        image: hrci_stage
        ports:
            - "8888:5000"
        environment:
            - ISENV=pro
        volumes:
            - ./:/code
            - "/home/ubuntu/pro/HRCI/uploads:/app/uploads"
            - "/home/ubuntu/pro/HRCI/Template:/app/Template"
        restart: always
        networks:
            - backend-network
networks:
    backend-network:
        driver: bridge
