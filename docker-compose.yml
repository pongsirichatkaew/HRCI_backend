
version: '2'

services:
    hrci_state:
        image: hrci_back
        container_name: hrci_state
        ports:
            - "5000:5000"
        environment:
            - ISENV=pro
        volumes:
            - ./:/code
            - "/home/ubuntu/pro/HRCI/uploads:/app/uploads"
            - "/home/ubuntu/pro/HRCI/Template:/app/Template"
        restart: always
        networks:
            - backend-network
networks:
    backend-network-state:
        driver: bridge
